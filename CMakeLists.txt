cmake_minimum_required(VERSION 2.8)
project( picts-psnr )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( picts-psnr main.cpp )
target_link_libraries( picts-psnr ${OpenCV_LIBS} )
target_compile_features(picts-psnr PRIVATE cxx_range_for)